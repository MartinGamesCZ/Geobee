{
  "name": "geobee",
  "module": "src/index.ts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.0.0",
  "scripts": {
    "prepack": "rm -rf dist && bun build --minify --outdir dist src/index.ts && tsc",
    "post": "npm publish"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
